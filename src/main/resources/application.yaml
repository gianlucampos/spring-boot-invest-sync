spring:
  application:
    name: spring-boot-invest-sync

google:
  sheets:
    credentials: ${GOOGLE_SHEETS_CREDENTIALS}

integrations:
  apis:
    br-api:
      url: https://brapi.dev/api/quote/
      token: ${BR_API_TOKEN}
    usa-api:
      url: https://stockprices.dev/api/stocks/
      token:
