spring:
  application:
    name: spring-boot-invest-sync
  cloud:
    function:
      definition: updatePortfolio

google:
  sheets:
    credentials-file: ${GOOGLE_CREDENTIALS_PATH}

integrations:
  br-api:
    url: https://brapi.dev/api/quote/
    token: ${BR_API_TOKEN}

  usa-api:
    url: https://stockprices.dev/api/stocks/
    token:
